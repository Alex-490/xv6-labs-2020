1、a开头的寄存器保存函数参数。a2保存13。
2、main调用了f，f调用了g，46行里直接将f调用g的过程省略，结果保存在a1。
3、49行ra=pc指向的下一条指令地址0x34+0x0左移12位=0x34。50行ra+1536=0x634。
4、0x34？auipc ra,0x0 这条指令用到的pc值究竟是这条指令的地址0x30还是下条指令的0x34？
5、57616的十六进制表示为0xE110，i的小端保存为72-6c-64-00，ascii码对应字符为r-l-d-'\0'。
    因此输出将是HE110 World。如果是大端存储，i=0x726c6400，不需要将57616更改。
6、取决于寄存器a2之前保存的数据。